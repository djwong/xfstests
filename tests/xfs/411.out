QA output created by 411
Format and populate
Fuzz refcountbt
scrub didn't fail with numrecs = zeroes.
offline re-scrub (1) with numrecs = zeroes.
re-repair failed (1) with numrecs = zeroes.
scrub didn't fail with numrecs = middlebit.
offline re-scrub (1) with numrecs = middlebit.
re-repair failed (1) with numrecs = middlebit.
scrub didn't fail with lsn = zeroes.
scrub didn't fail with lsn = ones.
scrub didn't fail with lsn = firstbit.
scrub didn't fail with lsn = middlebit.
scrub didn't fail with lsn = lastbit.
scrub didn't fail with lsn = add.
offline re-scrub (1) with lsn = add.
re-repair failed (1) with lsn = add.
scrub didn't fail with lsn = sub.
scrub didn't fail with lsn = random.
scrub didn't fail with recs[1].refcount = lastbit.
Done fuzzing refcountbt
